<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/output.css">
    <%-include('partials/favicon') %>
    <title>My Food Journal</title>
</head>
<body>
    <%-include('partials/nav') %>
    <main class="flex flex-col gap-1 justify-center mx-auto max-w-52 w-[80vw]">
        <div class="heading flex flex-col mb-1">
            <%-include('partials/icon', {src:"/assets/sickness-icon.png", 
            alt:"An icon of an ill face, linking to the create sickness page", addClass: "h-4 mx-auto p-0"}) %>
            <h3 class="mx-auto mb-1">New Sickness</h3>
            <form action="#", method="POST" class="border-style border-light-primary-200 flex flex-col gap-0.5">
                <fieldset>
                    <label for="name">Illness:</label>
                    <input type="text" name="name" class="border-underline">
                </fieldset>
                <fieldset>
                    <label for="time">Time:</label>
                    <input type="time" name="time" class="border-underline">
                </fieldset>
                <fieldset>
                    <label for="desc">Description:</label>
                    <input type="text" name="desc" class="border-underline">
                </fieldset>
                <fieldset class="addList">
                    <p>Symptoms:</p>
                    <div id="symptoms"></div>
                    <button type="button" id="addSymptom" class="w-fit mx-auto">Add symptom</button>
                </fieldset>
                <fieldset class="addList">
                    <p>Associated Foods:</p>
                    <div id="associatedFoods"></div>
                    <button type="button" id="addAssociatedFood" class="w-fit mx-auto">Add association</button>
                </fieldset>
                <input type="submit" value="Add Sickness" class="flex mx-auto my-1">
            </form>
        </div>
    </main>
    <script type="module">
        import {insertItem} from "./js/insertFormListItem.js";
        const scripts = new insertItem();

        scripts.listenForSymptomAdd("addSymptom");
        scripts.listenForAssociationAdd("addAssociatedFood");
    </script>
</body>
</html>